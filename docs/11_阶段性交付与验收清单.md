# 11 阶段性交付与验收清单

## 1. 阶段一：模块化单体闭环（当前已交付）

### 交付物
- [x] 后端 Spring Boot 3 多模块工程
- [x] 前端门户 + 管理后台
- [x] 统一响应 / 异常 / 日志
- [x] 配置文件（application.yml + application-dev.yml）
- [x] Docker Compose（MySQL + Redis）
- [x] 数据库脚本（scripts/schema.sql）

### 验收点
- 后端能启动：`mvn -pl pureblog-app -am spring-boot:run`
- 前端能启动：`npm run dev`
- 健康检查可访问：`GET /actuator/health` 返回 200 + `status: UP`
- 登录接口可访问：`POST /api/auth/login {username:"admin",password:"admin123"}`
- 前后端联调通：门户登录 → 发布文章 → 搜索 → 详情

---

## 2. 后续阶段（按需）

### 2.1 文件上传
- [ ] MinIO 容器
- [ ] 上传接口
- [ ] 前端上传组件
- [ ] 接入文章封面 / 正文图片

### 2.2 限流与防刷
- [ ] 登录限流
- [ ] 评论限流
- [ ] 通用 Redis 限流注解

### 2.3 可观测
- [ ] Micrometer
- [ ] Prometheus
- [ ] Grafana 看板
- [ ] 关键路径耗时埋点

### 2.4 跨进程消息
- [ ] Redis Stream 或 Kafka
- [ ] 消费幂等表
- [ ] Outbox 模式

### 2.5 服务化
- [ ] 拆分 search / notification
- [ ] 引入 Spring Cloud OpenFeign / Dubbo
- [ ] 注册中心 / 配置中心

### 2.6 压测与调优
- [ ] k6 / JMeter 脚本
- [ ] 压测报告
- [ ] JVM 调优记录

---

## 3. 最终交付清单

最终建议整理出以下成果用于简历与面试：

1. [x] 项目源码仓库（本仓库）
2. [x] 数据库 SQL（`scripts/schema.sql` + `docs/04_数据库设计.sql`）
3. [x] Docker Compose（`deploy/docker-compose.dev.yml`）
4. [x] 接口文档（`docs/06_API接口清单.md`）
5. [x] 架构设计（`docs/02_系统架构设计.md`）
6. [ ] 缓存 / 搜索 / 事件设计说明（可单独写）
7. [ ] JVM 调优记录
8. [ ] 压测报告
9. [ ] 项目演示视频 / 截图

---

## 4. 面试展示顺序建议

1. 项目定位（围绕发布 / 搜索 / 热点三条链路）
2. 架构图（单仓多模块 + 进程内事件）
3. 数据模型（关键表 + 关系）
4. 发布链路（写完事务 → 派发事件 → 搜索同步）
5. 搜索链路（ES 加权 + 高亮 + 索引重建）
6. 热点链路（缓存 + 热度 ZSet + 定时兜底）
7. 一致性策略（Cache Aside + 事件驱动）
8. 鉴权设计（JWT + 双 Token + 黑名单）
9. 后续演进（MinIO / Kafka / 服务化）

---

## 5. 验收脚本参考

```bash
# 1. 启动基础设施
cd deploy
docker compose -f docker-compose.dev.yml up -d

# 2. 启动 Elasticsearch（独立）
docker run -d --name pureblog-es \
  -p 9200:9200 -e "discovery.type=single-node" \
  docker.elastic.co/elasticsearch/elasticsearch:8.13.0

# 3. 初始化数据库
mysql -uroot -p123456 < ../scripts/schema.sql

# 4. 启动后端
cd ../backend
mvn -pl pureblog-app -am spring-boot:run

# 5. 验证
curl http://localhost:8080/actuator/health
curl -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'

# 6. 启动前端
cd ../frontend/pureblog-portal
npm install && npm run dev

cd ../pureblog-admin-web
npm install && npm run dev
```
